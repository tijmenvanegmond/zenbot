version: "3.8"

services:
  zenbot:
    build: .
    container_name: zenbot
    ports:
      - "3001:8080"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - TTS_PROVIDER=linux
    restart: unless-stopped
    volumes:
      - /tmp/zenbot:/tmp/zenbot
